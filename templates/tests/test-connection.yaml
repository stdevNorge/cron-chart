apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "genericTest.fullname" . }}-test-connection"
  labels:
    {{- include "genericTest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "genericTest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
